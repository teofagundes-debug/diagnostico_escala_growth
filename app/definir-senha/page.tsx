'use client';
import {useEffect,useState} from 'react';
export default function DefinirSenha(){const [token,setToken]=useState(''),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{const hash=new URLSearchParams(location.hash.slice(1)),query=new URLSearchParams(location.search);setToken(hash.get('access_token')||query.get('access_token')||'')},[]);const save=async()=>{if(password.length<8){setMessage('A senha deve possuir pelo menos 8 caracteres.');return}if(password!==confirm){setMessage('As senhas não coincidem.');return}setBusy(true);const r=await fetch('/api/client-access',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({access_token:token,password})}),x=await r.json();if(r.ok){setMessage('Senha criada com sucesso. Você já pode entrar na Central.');setTimeout(()=>location.href='/login',1200)}else setMessage(x.error||'Não foi possível criar a senha.');setBusy(false)};return <main className="login-page"><section className="login-card"><img src="/logo-escala-vendas.png" alt="Escala Vendas"/><span className="eyebrow">Central Escala Growth</span><h1>Crie sua senha</h1><p>Defina uma senha segura para acessar a jornada da sua empresa.</p>{!token?<p className="error">Este convite é inválido ou não possui o token de acesso.</p>:<><label>Nova senha<input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirmar senha<input type="password" minLength={8} value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><button className="btn btn-primary" disabled={busy} onClick={save}>{busy?'Salvando...':'CRIAR MINHA SENHA E ACESSAR'}</button></>}{message&&<p className={message.includes('sucesso')?'success':'error'}>{message}</p>}</section></main>}

