'use client';
import {useState} from 'react';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const login=async()=>{const r=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(r.ok)location.href='/central';else setError((await r.json()).error)};return <main className="admin-login"><div className="admin-card"><span className="eyebrow">Central Escala Growth</span><h1>Entrar</h1><p>Área exclusiva da equipe Escala Vendas.</p><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>e.key==='Enter'&&login()}/></label><button className="btn btn-primary" onClick={login}>Entrar</button>{error&&<p className="error">{error}</p>}</div></main>}

