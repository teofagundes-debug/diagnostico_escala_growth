'use client';
import {useState} from 'react';
import {brandLogo} from '../../components/brand';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const login=async()=>{const r=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(r.ok){const session=await fetch('/api/auth',{cache:'no-store'}),data=await session.json().catch(()=>({}));if(data.role==='master')location.href='/central';else if(data.role==='cliente')location.href='/portal';else{await fetch('/api/auth',{method:'DELETE'});setError('Esta conta ainda n?o possui um tipo de usu?rio definido.')}}else setError((await r.json()).error)};return <main className="admin-login premium-login"><div className="admin-card"><img src={brandLogo} alt="Escala Vendas"/><span className="eyebrow">Método Escala Growth</span><h1>Bem-vindo à Central Escala Growth.</h1><p>Uma visão executiva para acompanhar empresas, preparar decisões e comprovar evolução.</p><label>E-mail<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>e.key==='Enter'&&login()}/></label><button className="btn btn-primary" onClick={login}>Entrar</button>{error&&<p className="error">{error}</p>}</div></main>}
