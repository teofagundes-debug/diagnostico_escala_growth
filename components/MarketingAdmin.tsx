'use client';
import {useEffect,useState} from 'react';
const money=(v:any)=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
const scenarios=[['local','Local','Empresas que atendem apenas sua cidade.'],['regional','Regional','Empresas que atendem todo o estado.'],['nacional','Nacional','Empresas que atendem todo o Brasil.'],['personalizado','Personalizado','Valor livre definido pelo consultor.']];
export function MarketingParameters(){const [rows,setRows]=useState<any[]>([]),[error,setError]=useState(''),[message,setMessage]=useState('');const load=async()=>{const r=await fetch('/api/commercial',{cache:'no-store'}),x=await r.json();if(r.ok)setRows(x.marketing||[]);else setError(x.error)};useEffect(()=>{load()},[]);const edit=(i:number,key:string,value:any)=>setRows(rows.map((x,n)=>n===i?{...x,[key]:value}:x));const scenario=(i:number,key:string,value:number)=>setRows(rows.map((x,n)=>n===i?{...x,investimentos:{...(x.investimentos||{}),[key]:value}}:x));const save=async()=>{setError('');const r=await fetch('/api/commercial',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'marketing',platforms:rows})}),x=await r.json();if(!r.ok)setError(x.error);else{setMessage('Parâmetros de marketing salvos com sucesso.');load()}};if(!rows.length&&!error)return <div className="loading-state">Preparando parâmetros de marketing...</div>;return <><section className="admin-section"><span className="eyebrow">Administração · acesso Master</span><h2>Parâmetros de Marketing</h2><p>Implantação, gestão e investimento em mídia permanecem separados. A estrutura aceita novas plataformas futuramente.</p>{error&&<p className="error">{error}</p>}</section><div className="marketing-platforms">{rows.map((p,i)=><section className="admin-section" key={p.plataforma}><h2>{p.plataforma}</h2><div className="marketing-main-fields"><label>Quantidade de UI<input type="number" min="0" value={p.ui} onChange={e=>edit(i,'ui',Number(e.target.value))}/></label><label>Valor da Gestão Mensal<input type="number" min="0" step="0.01" value={p.valor_gestao_mensal} onChange={e=>edit(i,'valor_gestao_mensal',Number(e.target.value))}/></label><label>Responsável padrão<select value={p.responsavel_padrao} onChange={e=>edit(i,'responsavel_padrao',e.target.value)}>{['Escala Vendas','Cliente','Parceiro'].map(x=><option key={x}>{x}</option>)}</select></label></div><h3>Investimento Inicial Recomendado por Mês</h3><div className="marketing-scenarios">{scenarios.map(([key,title,description])=><label key={key}><b>{title}</b><small>{description}</small><input type="number" min="0" step="0.01" value={p.investimentos?.[key]||0} onChange={e=>scenario(i,key,Number(e.target.value))}/><span>{money(p.investimentos?.[key])}</span></label>)}</div></section>)}</div><section className="admin-section marketing-rule"><h3>Regra financeira</h3><p>O investimento em Google e Meta é realizado diretamente nas plataformas, pertence ao cliente e nunca faz parte do faturamento da Escala Vendas.</p><button className="btn btn-primary" onClick={save}>Salvar parâmetros de marketing</button>{message&&<p className="success">{message}</p>}</section></>}
