'use client';
import {useEffect,useState} from 'react';
export function CampaignPlanningSummary({companyId,projectId}:{companyId:string;projectId:string}){const [plans,setPlans]=useState<any[]>([]);useEffect(()=>{fetch(`/api/commercial?empresa=${encodeURIComponent(companyId)}&projeto=${encodeURIComponent(projectId)}`,{cache:'no-store'}).then(r=>r.ok?r.json():null).then(data=>setPlans(Array.isArray(data?.plans)?data.plans:[])).catch(()=>setPlans([]))},[companyId,projectId]);if(!plans.length)return null;return <section className="admin-section"><span className="eyebrow">Implantação</span><h2>Marketing</h2><p>Resumo operacional somente para leitura. As configurações são realizadas no Centro de Implantação.</p><div className="detail-metrics">{plans.map(plan=><div key={plan.id}><small>{plan.plataforma}</small><b>{plan.status}</b><span>Investimento aprovado: {Number(plan.investimento_aprovado||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}</span><span>Responsável: {plan.executor||plan.responsavel_configuracao||'A definir'}</span></div>)}</div></section>}
